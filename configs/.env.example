# =============================================================================
# Variables de entorno — Secretos reales (api-gox-gh)
# =============================================================================
# Copia este archivo como configs/.env.local (o usa el symlink a
# ~/.indomito-hub/environments/.env.local) y completa con tus credenciales.
# NUNCA commitear .env.local — está protegido por .gitignore.
# =============================================================================

# --- Bootstrap (comunes a todos los servicios) ---
AWS_PROFILE=pa-dev
AWS_REGION=us-east-1
